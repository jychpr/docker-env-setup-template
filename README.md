# docker-env-setup-template

code to follow 
- docker-compose up --> create, update, also run the container. setting up environment after Dockerfile, requirements.txt, and docker-compose.yaml has been created